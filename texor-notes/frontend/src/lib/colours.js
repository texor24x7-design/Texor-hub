/**
 * The colours a note or a label can be.
 *
 * The same list the server enforces in `models/Note.js`, and the same names the
 * highlighter uses inside a document — so "yellow" means one colour in this
 * product rather than two that nearly match.
 */
export const NOTE_COLOURS = [
  'default', 'yellow', 'green', 'blue', 'pink', 'purple', 'red', 'orange', 'grey',
];

export default NOTE_COLOURS;
